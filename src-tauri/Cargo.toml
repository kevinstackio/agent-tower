[package]
name = "helloword"
version = "0.1.0"
description = "A minimal HelloWord desktop app built with Tauri and React."
authors = ["Codex"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2.0.0", features = [] }

[dependencies]
tauri = { version = "2.0.0", features = [] }
